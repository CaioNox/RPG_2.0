// Package: engine.input
// Purpose: Central place to process raw input (keyboard, mouse, controller) each frame.
// Usage: Called by the main loop or state to handle player input and translate it into game actions.
// Relationships: May emit events or call state methods based on input.
package engine.input;

public class InputHandler {
    public void processInput() {
        // placeholder
    }
}
