// Package: game.logic
// Purpose: Coordinates battles between players and enemies (high-level flow controller).
// Usage: Entry point for starting combats; delegates calculations to helpers like `DamageCalculator`.
// Relationships: Uses `Player`, `Enemy`, and other rule/turn systems.
package game.logic;

import game.entities.Enemy;
import game.entities.Player;

public class BattleSystem {
    public void startBattle(Player p, Enemy e) {
        // minimal placeholder
    }
}
